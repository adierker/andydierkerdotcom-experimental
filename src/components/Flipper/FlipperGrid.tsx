import { ReactElement } from 'react'

interface FlipperGridProps {}

export const FlipperGrid = ({}: FlipperGridProps): ReactElement => {
  return <div>flipper grid</div>
}

export default FlipperGrid
